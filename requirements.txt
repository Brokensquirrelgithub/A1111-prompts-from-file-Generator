python-dotenv>=0.19.0
pathlib2>=2.3.7
# Tkinter is part of Python's standard library but may need to be installed separately on some Linux distributions
# Example for Debian/Ubuntu: sudo apt-get install python3-tk

# Required for file operations and system interactions
platformdirs>=2.4.0

# Required for JSON operations
json5>=0.9.5

# Required for file system operations
shutilwhich>=1.1.0
