python-dotenv>=0.19.0
pathlib2>=2.3.7
# Tkinter is part of Python's standard library but may need to be installed separately on some Linux distributions
# Example for Debian/Ubuntu: sudo apt-get install python3-tk
